# AGENTS.md

本文档定义本仓库中 Coding Agent 必须遵守的工程原则。

## 文档优先级

实现时按以下优先级理解系统设计：

1. `docs/ARCHITECTURE.md`：顶层架构与不可破坏的系统边界
2. `docs/phases/PHASE_*.md`：当前阶段的目标、接口与验收标准
3. 模块内 README / ADR：具体设计决策
4. 现有代码：实现参考

涉及架构或跨模块修改时，必须先阅读对应设计文档，再实施修改；不要仅根据当前代码推断目标架构。
当现有代码与当前 Phase 文档冲突时，优先按设计文档重构并清理旧实现，不以“保持现有行为”为默认目标。

## 核心工作原则

1. **先理解再修改**
   - 修改代码前先阅读相关模块、接口和架构文档，理解现有数据流与依赖关系。
   - 不根据文件名或局部代码猜测系统行为。
   - 涉及跨模块设计时，优先确认 `docs/ARCHITECTURE.md` 及对应 Phase 文档。

2. **保持架构边界**
   - 不为了快速实现功能绕过既有模块边界。
   - 不跨层直接访问其他模块的 ORM、内部实现或基础设施对象。
   - 新能力应放入其正确的 Domain / Application / Agent / Memory / Tool / Infrastructure 层。

3. **避免无意义兼容与兜底**
   - 不为已经废弃的代码、接口或数据结构保留无明确需求的兼容逻辑。
   - 重构时应彻底删除被替代的旧实现、死代码和重复路径。
   - 不通过静默 fallback、吞异常、默认值等方式掩盖真正的错误。
   - 只有在业务语义明确要求降级时才实现 fallback，并说明触发条件。

4. **测试以真实风险为中心**
   - 不为简单 CRUD、数据模型、薄封装或显而易见的代码机械补单元测试。
   - 优先编写集成测试和场景测试，验证真实调用链和关键业务行为。
   - 对复杂纯逻辑、关键状态转换、算法和高风险边界编写必要的单元测试。
   - 不为了提高测试数量或覆盖率而编写低价值测试。

5. **注释表达设计意图**
   - 注释和 Prompt 一律使用中文。
   - 核心类、数据模型的重要字段应说明业务含义。
   - 对关键业务流程、状态转换、并发边界和不直观的设计决策提供准确注释。
   - 不逐行翻译代码，不为显而易见的实现添加无意义注释。
   - 注释应解释“为什么这样设计”以及必要的业务语义，而不仅是“代码做了什么”。

6. **保持类型清晰**
   - 公共接口、函数参数、返回值、领域模型和重要数据结构必须提供完整类型注解。
   - 对类型不明显或容易产生歧义的变量显式标注类型。
   - 不要求为能够自然推断类型的简单局部变量机械添加类型注解。
   - 避免滥用 `Any`；只有边界确实无法静态表达时使用。

7. **不要过度设计**
   - 不因为未来“可能需要”而提前引入复杂抽象、兼容层、中间件或扩展点。
   - 已在架构文档中明确的长期边界应一次设计正确；具体实现只做到当前阶段真正需要的程度。
   - 优先选择简单、明确、可维护的实现，而不是为了展示技术复杂度增加组件。

8. **错误应尽早暴露**
   - 对违反系统不变量、非法状态和错误配置优先 fail fast。
   - 不使用宽泛的 `except Exception` 静默恢复，除非位于明确的系统边界并进行了错误归一化。
   - 不伪造成功结果，不用空对象或默认数据掩盖执行失败。

9. **保持代码库整洁**
   - 新实现完成后同步删除不再使用的 import、接口、文件、配置和旧路径。
   - 不保留注释掉的大段旧代码。
   - 不创建没有实际用途的空抽象层、空目录或占位实现。
   - 避免一个能力存在两套并行实现。

10. **改动必须与当前任务相关**
    - 可以主动修复实现当前任务过程中发现的直接相关问题。
    - 不在没有必要的情况下大规模重写无关模块。
    - 如果发现架构文档本身存在明显问题，应指出问题，而不是私自改变顶层设计。
